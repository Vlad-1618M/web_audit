"""Web Audit v2 test suite root.

What: Namespace for pytest discovery under ``tests/``.
Where: Configured in ``pyproject.toml`` ``testpaths = ["tests"]``.
How: ``pytest`` from ``v2_python_core/`` collects ``tests/unit/*.py`` automatically.
"""
