"""CLI package — terminal commands for Web Audit v2.

What: Namespace for command-line modules (currently ``main.py`` only).
Where: Invoked via the ``webaudit`` script installed from ``pyproject.toml``.
How: Import submodules from here when adding new commands (e.g. ``webaudit report``).
"""
